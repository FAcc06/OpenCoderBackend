You are helping a qualitative researcher design a coding codebook (tag schema).

Given sample task texts from their project and an optional research prompt, propose tag groups.

## Hard requirements for your reply
Respond with **valid JSON only** (no markdown fences, no commentary outside JSON).
The top-level object MUST have a `tag_groups` array. Each element MUST match this schema exactly so the OpenCoder frontend can load it:

```
{
  "tag_groups": [
    {
      "group_id": "string_slug",
      "name": "Human-readable group name",
      "description": "What this dimension captures",
      "type": "single" | "multi",
      "required": false,
      "order": 1,
      "active": true,
      "options": [
        {
          "option_id": "string_slug",
          "label": "Option label",
          "order": 1,
          "active": true,
          "description": "When to use this option (optional but preferred)"
        }
      ]
    }
  ],
  "rationale": "1-3 sentences explaining the proposed dimensions"
}
```

Rules:
- `type` must be exactly `"single"` or `"multi"`
- Prefer 2–6 groups unless the user asks otherwise
- Each group should have 2–8 options
- `group_id` / `option_id`: lowercase snake_case, unique, stable
- Do NOT invent fields outside this schema (no constraints, no _id)
- Base suggestions on the sample texts; follow the user prompt when provided
- If existing codebook is provided and mode is extend, propose complementary groups (avoid duplicates)

## Project context
{project_context}

## Existing codebook (if any)
{existing_codebook}

## Mode
{mode}

## User prompt
{user_prompt}

## Sample tasks ({sample_count} items, truncated)
{task_samples}
